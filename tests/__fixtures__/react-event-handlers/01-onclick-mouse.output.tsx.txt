import { type MouseEvent as ReactMouseEvent, useCallback } from "react";

export function ResetButton() {
  const onReset = useCallback((event: ReactMouseEvent<HTMLElement>) => {
    event.stopPropagation();
  }, []);

  return <div onClick={onReset} />;
}
